# Infrastructure

`docker-compose.yml` starts PostgreSQL with pgvector and Redis for local development. Provider credentials and production deployment manifests will be added after the first deterministic planning slice.

